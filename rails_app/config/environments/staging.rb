# frozen_string_literal: true

require Rails.root.join('config/environments/production')

Rails.application.configure do
  # Staging configuration that differs from production configuration should be added here.
end
