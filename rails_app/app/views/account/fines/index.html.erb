<% @page_title = t('account.fines_and_fees.page_title', application_name: application_name) %>

<%= render BreadcrumbsComponent.new do |component|%>
  <% component.with_breadcrumb(href: root_path) { t('account.breadcrumbs.root') } %>
  <% component.with_breadcrumb(href: account_path) { t('account.breadcrumbs.base') } %>
  <% component.with_breadcrumb(active: true) { t('account.fines_and_fees.page_heading') } %>
<% end %>

<h1 class="page__heading"><%= t('account.fines_and_fees.page_heading') %></h1>
<p class="page__lede mb-3"><%= t('account.fines_and_fees.page_lede') %></p>

<%= render Account::FinesTableComponent.new(user: current_user) %>

<p class="callout">
  <strong><%= t('account.guide_callout') %></strong> <%= t('account.fines_and_fees.how_to_pay_html', url: t('urls.guides.finesfees') ) %>
</p>