<% breadcrumb t('account.breadcrumbs.base'), href: account_path %>
<% breadcrumb t('account.shelf.page_heading'), href: requests_path %>
<% breadcrumb tag.span(@entry.title || @entry.id, class: 'pl-line-clamp', style: '--pl-line-clamp: 1') %>

<%= render(Account::ShelfEntryComponent.new(@entry)) %>