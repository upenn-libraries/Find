<%
  # Copied from Blacklight v8.10.1
  # To pass user object into our own design system header, which is set in the CatalogController.
%>

<%= render blacklight_config.header_component.new(blacklight_config: blacklight_config, user: current_user) %>