<%# Copied template from Blacklight v9.0 to use local component that supports range search fields %>

<%= render(Catalog::AdvancedSearchFormComponent.new(
  url: search_action_url,
  classes: ['advanced'],
  params: search_state.params_for_search.except(:qt),
  response: @response
)) %>