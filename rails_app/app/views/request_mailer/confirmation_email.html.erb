<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Find Request Confirmation</title>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'>
  </head>
  <body>
    <h1>Your Find request has been submitted</h1>
    <p>
      You have successfully submitted a request for: <%= @outcome.item_desc %>
    </p>
    <p>
      <%= @outcome.fulfillment_desc %>
    </p>
    <% if @outcome.request.proxied? %>
    <p>
      This request was submitted on behalf of <strong><%= @outcome.request.patron.email %></strong>. This user will receive subsequent notifications about this request.
    </p>
    <% end %>
    <% if @outcome.confirmation_number.present? %>
    <p>
      Your confirmation number is <strong><%= @outcome.confirmation_number %></strong>. Please reference it when inquiring
      about your request status or about any other issues.
    </p>
    <% end %>
    <p>
      <% if @docdel %>
      Contact <%= mail_to Settings.fulfillment.docdel.email %> with changes or questions concerning your request.
      <% else %>
      For questions regarding your request, <%= link_to 'see our guides', t('urls.guides.circulation') %>.
      <% end %>
    </p>
  </body>
</html>
