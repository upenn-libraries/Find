<%
  # Copied from Blacklight version 8.1.0
  # Added alert-dismissable os that the layout puts
  # the close "X" in the upper right.
  # Also, the button was a weird element named button_tag
  # but we need it to be button to be accessible.
%>

<div class="alert alert-dismissible <%= @classes %>">
  <%= message %>
  <%= tag.button button_contents, type: "button", class: "btn-close",
                     data: { dismiss: "alert", bs_dismiss: "alert" },
                     aria: { label: "Close" } %>
</div>