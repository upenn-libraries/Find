<%
  # Copied from Blacklight version v8.10.1
  # added the close "X" in the upper right.
  # Additionally, sanitizing message so that we can include html.
%>

<div class="alert alert-dismissible <%= @classes %>">
  <%= sanitize(message) %>
  <%= tag.button button_contents, type: "button", class: "btn-close",
                     data: { dismiss: "alert", bs_dismiss: "alert" },
                     aria: { label: "Close" } %>
</div>