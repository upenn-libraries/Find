<%# Copied template from Blacklight v9.0 to customize layout and CSS class names %>

<span>
  <% if total == 1 %>
    <span><%= item_page_entry_info %></span>
  <% else %>
    <%= link_to_previous_document classes: 'btn btn-light' %>
    <span><%= item_page_entry_info %></span>
    <%= link_to_next_document classes: 'btn btn-light' %>
  <% end %>
</span>
