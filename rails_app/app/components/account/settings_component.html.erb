
<h2 class="h4 mt-4 mb-3"><%= t('account.settings.details_heading') %></h2>
<div class="table-responsive">
  <table class="table align-middle">
    <tbody>
      <tr>
        <th scope="row">
          Name
        </th>
        <td>
          <%= full_name %>
        </td>
      </tr>
      <tr>
        <th scope="row">
          Email
        </th>
        <td>
          <%= email %>
        </td>
      </tr>
      <tr>
        <th scope="row">
          <%= t('account.settings.terms.user_group') %>
        </th>
        <td>
          <%= alma_user_group %>
        </td>
      </tr>
    </tbody>
  </table>
</div>

<p class="callout mt-0">
  <strong><%= t('account.guide_callout') %></strong> <%= t('account.settings.update_details_html', url: t('urls.guides.account') ) %>
</p>

<h2 class="h4 mt-5 mb-3"><%= t('account.settings.terms.bbm_delivery_address') %></h2>
<% if bbm_delivery_address.present? %>
  <p>
    <% bbm_delivery_address.each do |ele| %>
      <%= ele %><br/>
    <% end %>
  </p>
<% else %>
  <div class="alert alert-light">
    <p class="mb-0"><%= t('account.settings.bbm_empty') %></p>
  </div>
<% end %>

<p class="callout">
  <strong><%= t('account.guide_callout') %></strong> <%= t('account.settings.set_your_address_html', url: t('urls.guides.bbm') ) %>
</p>

