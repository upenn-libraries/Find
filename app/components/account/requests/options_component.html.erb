<%= turbo_frame_tag 'options_frame' do %>
  <p><%= item.pid %></p>
  <ul class="list-group">
    <% if options.include? :pickup %>
      <% if options.include? :scan %>
        <li class="list-group-item">
          <%= radio_button_tag :delivery, 'scan' %>
          <%= label_tag :delivery_scan, 'Get a Scan' %>
        </li>
      <% end %>
      <% if options.include? :office %>
        <%= render(Account::Requests::Options::OfficeComponent.new(user_address: user_address)) %>
      <% end %>
      <%= render(Account::Requests::Options::PickupComponent
                   .new(default_pickup_location: default_pickup_location, options: options)) %>
      <% if options.include? :mail %>
        <%= render Account::Requests::Options::MailComponent.new %>
      <% end %>
    <% elsif options.include? :aeon %>
      <li class="list-group-item">
        This item is available for viewing in the special collections reading room by submitting a viewing request in
        the Aeon Special Collections system. You will need to create an account and request an appointment time to view
        the item in the reading room.
      </li>
    <% elsif options.include? :archives %>
      <li class="list-group-item">
        Pick the damn thing up at the archives.
      </li>
    <% end %>
    <%= render(Account::Requests::ButtonsComponent.new(options: options)) %>
  </ul>
<% end %>
