<% @validated_sources.each_with_index do |source, i| %>
  <turbo-frame id="additional-results__source-<%= i %>">
    <% component = Object.const_get("AdditionalResults::Sources::#{source.camelcase}Component") %>
    <%= render component.new(query: @query) %>
  </turbo-frame>
<% end %>