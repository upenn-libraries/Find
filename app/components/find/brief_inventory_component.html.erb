<ol class="holdings">
  <% if inventory.entries.any? %>
  <turbo-frame id="inventory_<%= id %>" src="<%= inventory_solr_document_path(id) %>" target="_top">
    <%= resource_link_entries %>
    <% inventory.entries.length.times do %>
      <li class="holding__item holding__item--loading placeholder-glow" role="status">
        <div class="placeholder-glow">
          <span class="visually-hidden"><%= t('inventory.entries.loading') %></span>
          <span class="placeholder col-3"></span>
        </div>
        <div class="placeholder-glow">
          <span class="placeholder col-1"></span>
          <span class="placeholder col-3"></span>
        </div>
      </li>
    <% end %>
  </turbo-frame>
    <% if inventory.remainder.positive? %>
    <li>
      <a href="<%= solr_document_path(@id) %>" class="holdings__other-link">
        <%= t('inventory.see_all', count: inventory.remainder, options: 'option'.pluralize(inventory.remainder)) %>
      </a>
    </li>
    <% end %>
  <% end %>
</ol>