
<%= render BreadcrumbsComponent.new do |component|%>
  <% component.with_breadcrumb(href: root_path) { t('account.breadcrumbs.root') } %>
  <% component.with_breadcrumb(href: account_path) { t('account.breadcrumbs.base') } %>
  <% component.with_breadcrumb(active: true) { t('account.settings.page_heading') } %>
<% end %>

<h1 class="page__heading"><%= t('account.settings.page_heading') %></h1>
<p class="page__lede mb-3"><%= t('account.settings.page_lede') %></p>

<%= render Account::SettingsComponent.new(user: current_user) %>