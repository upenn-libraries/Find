<%= render RequestComponent.new(mms_id: @mms_id,
                                holding_id: @holding_id,
                                holding_labels: @holding_labels,
                                item_labels: @item_labels,
                                user_id: current_user.uid,
                                user_group: @user_group) %>