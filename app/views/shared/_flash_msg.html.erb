<%
  # Copied from Blacklight version 8.1.0
  # See flash_message_component for reasoning.
%>

<div class="flash_messages">
  <% [:success, :notice, :error, :alert].each do |type| %>
    <%= render(Find::System::FlashMessageComponent.with_collection(Array.wrap(flash[type]), type: type)) if flash[type] %>
  <% end %>
</div>